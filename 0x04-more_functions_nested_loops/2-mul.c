#include "main.h"

/**
 *mul - Multiplies two integers
 *@a: The first integer
 *@b: The second integer
 *Return: (0) if successfull, (1) if otherwise
 **/

int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
